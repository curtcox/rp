# rp init

```
rp init
```

Create a `.rp/` project in the current directory: the directory layout and a
starter config that `plan`, `achieve`, and the scaffolding commands build on.

## Example

<!-- rp-example: id=init-basic cwd=empty status=todo -->
```console
$ rp init
# (output to be captured)
```

## After init

<!-- rp-example: id=init-then-resources cwd=empty status=todo -->
```console
$ rp init
$ rp resources
# (output to be captured)
```

## See also

- [scaffold](scaffold.md) — add capabilities, goals, policies, and resources.
- [config reference](../config/reference.md) — the `.rp/` YAML schema.
