# Bug: greet returns the wrong string

The `greet()` function in `greet.py` should return `"Hello, rp!"` but
currently returns `"Hello, World!"`.

Expected behavior:

- `greet()` returns `"Hello, rp!"`

Actual behavior:

- `greet()` returns `"Hello, World!"`
